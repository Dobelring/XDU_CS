整体功能没问题，但整点蜂鸣器不响，原因不明。

更改`showtime`中循环次数（`i < 10`）可以调节 1 秒钟的真实物理时长，也就是会调节时间跳动的速度。

```
 for (i = 0; i < 10; i++) {
        a[0] += 0x80;
        delay(5, a);
        a[0] -= 0x80;
        delay(5, a);
    }
```